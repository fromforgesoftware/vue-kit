name: Release
on:
  release:
    types: [published]
  workflow_dispatch:
env:
  HUSKY: '0'
  NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          path: vue-kit
      - uses: actions/checkout@v4
        with:
          repository: fromforgesoftware/ts-kit
          path: ts-kit
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: 'https://npm.pkg.github.com'
          scope: '@fromforgesoftware'
      - run: cd ts-kit && npm install && npm run build
      - run: cd vue-kit && npm install --legacy-peer-deps && npm run build
      - name: Set version from release tag
        if: github.event_name == 'release'
        env:
          TAG: ${{ github.event.release.tag_name }}
        run: cd vue-kit && npm version "${TAG#v}" --no-git-tag-version --allow-same-version
      - name: Publish (idempotent) to GitHub Packages
        run: |
          set -euo pipefail
          cd vue-kit
          name=$(node -p "require('./package.json').name")
          V=$(node -p "require('./package.json').version")
          if npm view "${name}@${V}" version --registry=https://npm.pkg.github.com/ >/dev/null 2>&1; then echo "already published"; exit 0; fi
          npm publish --access=public
